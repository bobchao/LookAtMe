class TimerSettings {
  final bool darkMode;
  final bool soundEnabled;
  final Color timerColor;
  
  const TimerSettings({
    this.darkMode = false,
    this.soundEnabled = true,
    this.timerColor = Colors.green,
  });
} 